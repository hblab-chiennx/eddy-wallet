Every grouped block of content sits in a Card.

```jsx
<Card tone="paper">…</Card>
<Card tone="brand" sticker radius="var(--radius-xl)">…</Card>
```

`tone="parent"` tints a block as parent-only territory. Radius: `--radius-lg` in Parent Mode, `--radius-xl` in Child Mode.
