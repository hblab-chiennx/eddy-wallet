Icon-only tap target for top bars and row actions.

```jsx
<IconButton icon="bell" label="Notifications" />
<IconButton icon="settings" label="Settings" tone="outline" />
```

`label` is mandatory. Keep `size` >= 44 anywhere a child taps.
