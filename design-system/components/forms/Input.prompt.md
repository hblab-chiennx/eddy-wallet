Labelled text field. Border thickens to brand teal on focus.

```jsx
<Input label="Weekly allowance" prefix="$" inputMode="decimal" hint="Paid every Sunday morning." />
<Input label="Child's name" error="Give Eddy a name to use." />
```

For money entry prefer AmountField. Errors are written as fixes, not scolds.
