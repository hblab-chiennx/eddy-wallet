Segmented filter inside a screen — not for top-level navigation (use TabBar/TopBar).

```jsx
<Tabs value={filter} onChange={setFilter} items={["All", "Allowance", "Spending", "Loans", "Lessons"]} />
```
