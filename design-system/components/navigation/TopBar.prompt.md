Screen header. The Sky rail + shield is the persistent signal that the user is in Parent Mode.

```jsx
<TopBar mode="parent" title="Parent-only actions" back onBack={...} actions={<IconButton icon="bell" label="Alerts" />} />
```
