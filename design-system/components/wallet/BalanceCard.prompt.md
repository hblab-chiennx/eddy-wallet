The hero balance. One per screen, always the first thing below the header.

```jsx
<BalanceCard owner="Mia's wallet" balance={42.5} caption="Allowance lands Sunday — 3 days to go."
  chips={[{ icon: "hand-coins", label: "$5 weekly" }, { icon: "percent", label: "2% on savings" }]} />
```

A watermark piggy-bank glyph sits bottom-right; don't add other decoration.
