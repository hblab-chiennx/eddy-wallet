A loan with repayment progress. Read-only in Child Mode; parents get actions in `children`.

```jsx
<LoanCard reason="Lego set" borrowed={20} repaid={12} dueLabel="$2 every Sunday · 4 left" status="on-track" />
```

Overdue switches the track to Berry but the copy never shames — it states what to do next.
