Flags a parent privilege. Non-negotiable: every parent-only control carries this marker wherever it appears, in both modes.

```jsx
<ParentOnlyChip />
<ParentOnlyChip variant="banner" label="Parent-only actions — Mia can see these, not tap them" />
```

Always Sky blue + lock/shield. Never use Sky for anything else.
