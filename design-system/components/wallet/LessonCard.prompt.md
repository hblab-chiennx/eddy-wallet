A lesson in the learn track. Lessons unlock in order: exactly one `available`, the rest `done` or `locked`.

```jsx
<LessonCard index={3} title="What is interest?" blurb="Money that grows while it waits." minutes={3} state="available" />
```

Grape = learning. Locked cards lose their shadow and go flat — visibly not pressable.
